name: Laravel

on:
  push:
    branches: [ deploy ]
  pull_request:
    branches: [ deploy ]

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
    - name: Deploy to production
      uses: appleboy/ssh-action@master
      with:
        username: root
        host: ovz1.j1121565.m2oon.vps.myjino.ru
        password: ${{ secrets.SSH_PASSWORD }}
        port: 49346
        script: 'echo $USER && cd /var/www/requests_backend && chmod +x server_deploy.sh && ./server_deploy.sh'
